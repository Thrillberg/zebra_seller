<h2><%= @product.name %></h2>

<%= @product.description %><br />
<%= @product.price %><br />
<% if current_user %>
  <%= form_tag product_add_to_cart_path(@product) do %>
    <% submit_tag "Buy a #{@product.name}" %>
  <% end %>
<% else %>
  Log in to buy!
<% end %>

<%= render 'shared/cart' %>
